import { DisciplinaPipe } from './disciplina.pipe';

describe('DisciplinaPipe', () => {
  it('create an instance', () => {
    const pipe = new DisciplinaPipe();
    expect(pipe).toBeTruthy();
  });
});
